FROM node:20-slim

WORKDIR /app

RUN echo '{"name":"silo-frontend","version":"1.0.0","scripts":{"dev":"echo frontend not ready yet"}}' > package.json

EXPOSE 3000

CMD ["sh", "-c", "echo 'Frontend coming soon' && tail -f /dev/null"]
